import React from 'react';
import { _models } from '../../scripts/Models';
import './Cart.css';

export default function Cart(props:_models) {

    return (
        <div id="cart">

        </div>
    )
}